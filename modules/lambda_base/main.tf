# Shared Lambda base module — packages and deploys a Python Lambda function with a
# pre-configured execution role and CloudWatch log group.

# TODO: archive_file (zip source_dir → deployment package)
# TODO: aws_cloudwatch_log_group (/aws/lambda/<function_name>, retention 7 days)
# TODO: aws_lambda_function
