# Shared VPC module — networking layer for scenarios that require private subnets
# (e.g., OpenSearch Serverless VPC endpoints).

# TODO: aws_vpc
# TODO: aws_subnet (public × 2, private × 2 across two AZs)
# TODO: aws_internet_gateway
# TODO: aws_nat_gateway + aws_eip
# TODO: aws_route_table + aws_route_table_association (public and private)
